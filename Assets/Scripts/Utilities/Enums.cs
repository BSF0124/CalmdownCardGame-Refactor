public static class Enums
{
    public enum SceneType { Manager = 0, MainMenu, WorldMap, Dual, NNG, CutScene }

    public enum DuelMode { Basic = 0, OneLess, ShellGame, FlipCards, Poker, Blackjack }
    
    public enum StageRule { Null = 0, Equalize, InverseRPS, NoDuplicate, SwapDeck, CopyDeck, RandomDeck}
}
